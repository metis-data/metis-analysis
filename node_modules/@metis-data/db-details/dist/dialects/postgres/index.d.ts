import DbDetails from "../db-details";
import { Connection, DbDetailsOptions } from "@types";
import { Client } from "pg";
export default class PGDbDetails extends DbDetails<any> {
    private client;
    private pgStructure;
    private tablesRowsAndPagesCount;
    private partialPgStatsTablesData;
    private dbDetails;
    private options;
    private setClientFromConnection;
    private setClientFromClientInstance;
    private setDataResourcesFromDb;
    aggregateResourcesToOneResource(): Promise<void>;
    getDbDetailsUseConnectionString(connection: Connection): Promise<any>;
    getDbDetails(clientOrConnectionConfig: Connection | Client, options?: DbDetailsOptions): Promise<any>;
}
